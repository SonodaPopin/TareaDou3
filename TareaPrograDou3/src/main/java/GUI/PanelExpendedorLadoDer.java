package GUI;

import javax.swing.*;
import java.awt.*;

/**
 * Clase que representa el panel del lado derecho del expendedor
 */
public class PanelExpendedorLadoDer extends JPanel {
    /**
     * Método constructor que crea el panel
     */
    PanelExpendedorLadoDer(){
        this.setBackground(Color.LIGHT_GRAY);
        this.setPreferredSize(new Dimension(25, 720));
    }
}
